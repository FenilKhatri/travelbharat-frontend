import ListingPage from "../../../components/listing/ListingPage";
import { festivalsConfig } from "../../../data/listingConfigs";

const Festivals = () => {
  return <ListingPage config={festivalsConfig} />;
};

export default Festivals;
